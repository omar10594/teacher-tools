services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: teacher-tools-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      N8N_WEBHOOK_URL: ${N8N_WEBHOOK_URL}
